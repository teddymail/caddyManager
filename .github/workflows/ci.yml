name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: 单元/API 测试
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: 运行测试
        run: npm test
